<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="match_parent">


    <ProgressBar
        android:id="@+id/loadingView"
        style="@style/Widget.AppCompat.ProgressBar"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:layout_gravity="center"
        android:indeterminateDrawable="@drawable/loading_iamge"
        android:indeterminateDuration="1000" />

    <TextView
        android:id="@+id/tvLoadImageFail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:drawableLeft="@drawable/load_image_error"
        android:drawableStart="@drawable/load_image_error"
        android:text="@string/load_image_error"
        android:textColor="@color/white"
        android:visibility="gone" />

    <ImageView
        android:id="@+id/ivFavouritePic"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="fitCenter" />


</FrameLayout>